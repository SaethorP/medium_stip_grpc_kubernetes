<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- Standalone helper; kept out of DebitCardApi.sln so 'dotnet test' ignores it. -->
  </PropertyGroup>

  <ItemGroup>
    <!-- Reuse the generated gRPC client/messages from the API project. -->
    <ProjectReference Include="..\..\DebitCardApi\DebitCardApi.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Grpc.Net.Client" Version="2.64.0" />
  </ItemGroup>

</Project>